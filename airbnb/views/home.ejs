<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airbnb Homes</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f7f7f7;
            padding: 20px;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .header h2 {
            color: #333;
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .homes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            list-style: none;
            margin-bottom: 40px;
        }

        .home-card {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
        }

        .home-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        }

        .card-image {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-bottom: 1px solid #eee;
        }

        .card-content {
            padding: 20px;
        }

        .house-name {
            font-size: 1.4rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 15px;
            line-height: 1.3;
        }

        .property-info {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #666;
            font-size: 0.95rem;
        }

        .info-label {
            font-weight: 600;
            color: #333;
            min-width: 70px;
        }

        .rating {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .star {
            color: #ffc107;
            font-size: 1.1rem;
        }

        .price {
            font-size: 1.2rem;
            font-weight: 700;
            color: #ff5a5f;
        }

        .add-home-btn {
            display: inline-block;
            background: linear-gradient(135deg, #ff5a5f, #ff385c);
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 90, 95, 0.3);
            text-align: center;
            margin: 0 auto;
            display: block;
            width: fit-content;
        }

        .add-home-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 90, 95, 0.4);
            background: linear-gradient(135deg, #ff385c, #e91e63);
        }

        .location-icon,
        .star-icon,
        .price-icon {
            width: 16px;
            height: 16px;
            opacity: 0.7;
        }

        @media (max-width: 768px) {
            .homes-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .header h2 {
                font-size: 2rem;
            }
            
            body {
                padding: 15px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h2>Our Registered Homes</h2>
        </div>
        
        <ul class="homes-grid">
            <% registereHomes.forEach(home => { %>
            <li class="home-card">
                <img src="<%= home.image %>" alt="<%= home.houseName %>" class="card-image" />
                <div class="card-content">
                    <h3 class="house-name"><%= home.houseName %></h3>
                    <div class="property-info">
                        <div class="info-item">
                            <span class="info-label">📍 Location:</span>
                            <span><%= home.location %></span>
                        </div>
                        <div class="info-item rating">
                            <span class="info-label">⭐ Rating:</span>
                            <span><%= home.rating %>/5</span>
                        </div>
                        <div class="info-item">
                            <span class="info-label">💰 Price:</span>
                            <span class="price">$<%= home.price %>/night</span>
                        </div>
                    </div>
                </div>
            </li>
            <% }) %>
        </ul>
        
        <a href="/host/add-home" class="add-home-btn">+ Add New Home</a>
    </div>
</body>

</html>