// const {add, subs, div, mult} = require('./app.js');

const math = require('../app.js');

console.log(math.add(2, 5));
console.log(math.mult(2, 5));
console.log(math.subs(2, 5));
console.log(math.div(2, 5)); 
